{
  "name": "utility",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*",
    "vitest": "^0.30.1"
  },
  "scripts": {
    "test": "vitest run"
  }
}
